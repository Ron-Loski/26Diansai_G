# 周期信号测量分析装置当前方案

> 文档性质：设计报告底稿  
> 更新时间：2026-07-30  
> 当前代码分支：`codex/ad9233-analyzer-verified-stage1`  
> 当前提交：`557342f`  

## 摘要

本装置用于测量和分析由基波与一个或两个整数次谐波组成的周期信号。被测信号
频率成分位于 10 kHz～500 kHz，峰峰值为 50 mV～250 mV；在提高要求中，
输入还叠加峰峰值为 200 mV、频率不低于 1 MHz 的单频干扰。

装置采用“模拟预处理 + FPGA高速采集 + MCU数字分析”的架构。模拟前端负责
50 Ω匹配、抑制1 MHz以上干扰并放大小信号；AD9233以25 MSPS完成模数转换；
FPGA完成ADC配置、13倍抽取、4096点缓存和SPI传输；STM32H743VIT6完成FFT
粗检测、谐波关系识别、联合最小二乘拟合以及峰峰值、真有效值、基频和各频谱
分量幅值计算。

当前AD9233、FPGA缓存、SPI链路和MCU算法已经上板跑通，并完成10～500 kHz
单音、双音和三音测试。模拟低通、放大器、整机复频响校准、强干扰测试和HMI
显示仍待完成，因此当前结果只能证明数字采集与分析链路可行，不能提前作为
整机抗干扰和5 mV精度的最终验收结论。

## 1. 设计任务与技术指标

### 1.1 输入信号

装置输入可表示为：

```text
u(t) = ub(t) + uJ(t)
```

其中：

- `ub(t)`为被测周期信号；
- `ub(t)`由基波和1个或2个整数次谐波组成，最多3个有效频率分量；
- `ub(t)`所有频率分量均处于10～500 kHz；
- `ub(t)`峰峰值为50～250 mV；
- `uJ(t)`为单频干扰；
- `uJ(t)`峰峰值为200 mV；
- `uJ(t)`频率不低于1 MHz；
- 信号源输出阻抗为50 Ω。

### 1.2 测量与显示指标

| ID | 指标 | 要求 | 当前状态 |
|---|---|---:|---|
| R1 | 波形显示 | 选择显示1个或3个完整周期 | MCU已能生成480点重构波形，HMI待接入 |
| R2 | 峰峰值 | 绝对误差不超过5 mV | 数字直连测试通过，整机待校准 |
| R3 | 真有效值 | 绝对误差不超过5 mV | 数字直连测试通过，整机待校准 |
| R4 | 基频 | 绝对误差不超过1 kHz | 已完成算法和数字链路验证 |
| R5 | 定性频谱 | 显示0～500 kHz正频率谱 | 算法已有频谱数据，HMI待接入 |
| R6 | 分量幅值 | 每个分量绝对误差不超过5 mV | 数字直连测试通过，整机待校准 |
| R7 | 频率分辨率 | 500 Hz | 内部FFT频格469.5 Hz，显示网格按500 Hz |
| R8 | 完成时间 | 每一项不超过2 s | 当前最慢三音处理约106 ms |
| R9 | 抗干扰 | 抑制200 mVpp、`fJ≥1 MHz`干扰 | 模拟低通和整机测试待完成 |

## 2. 总体方案

### 2.1 系统框图

```text
50 Ω BNC输入
        │
        ▼
7阶50 Ω模拟低通滤波器
        │
        ▼
约7.9倍低噪声放大器
        │
        ▼
50 Ω版AD9233，25 MSPS、12 bit
        │
        ▼
FPGA（EP4CE10F17C8）
  ├─ AD9233寄存器配置
  ├─ DCO域并行采样
  ├─ 每13点保留1点
  ├─ 4096点冻结缓存
  └─ SPI从机与CRC16
        │ SPI3
        ▼
STM32H743VIT6
  ├─ SPI DMA读取
  ├─ Hann窗4096点FFT
  ├─ 谐波阶数识别
  ├─ 联合最小二乘拟合
  ├─ Upp、Urms、f0、分量计算
  └─ 波形和频谱重构
        │
        ▼
串口日志 / 7寸HMI（待集成）
```

### 2.2 方案选择理由

| 方案 | 优点 | 主要问题 | 结论 |
|---|---|---|---|
| H743内部ADC完成采集和分析 | 硬件少、接口简单 | 采样质量、带宽和模拟输入不确定性较大 | 仅作备用 |
| AD9233 + FPGA采集 + H743分析 | 采样质量高，FPGA资源占用低，算法开发方便 | 需要稳定SPI链路和模拟前端 | 当前采用 |
| FPGA内完成FFT和全部计算 | 实时性强，减少MCU数据量 | EP4CE10 RAM和开发验证风险较大 | 当前不采用 |

当前架构将高速、确定性的采集和缓存交给FPGA，将浮点FFT、谐波识别、拟合和
显示交给STM32H743，既保留采样性能，又减少FPGA内部FFT带来的资源和调试风险。

## 3. 模拟前端设计

> 本节为当前设计值，模拟板尚未完成最终扫频验收。

### 3.1 50 Ω输入与低通滤波

为防止不低于1 MHz的强干扰在13倍抽取后混叠进入10～500 kHz分析带，抽取前
必须使用模拟抗混叠滤波器。当前设计采用50 Ω源、50 Ω负载的7阶0.1 dB
Chebyshev低通梯形网络：

```text
BNC输入
→ L1 = 15 µH 串联
→ C2 = 7.5 nF 对地
→ L3 = 27 µH 串联
→ C4 = 8.2 nF 对地
→ L5 = 27 µH 串联
→ C6 = 7.5 nF 对地
→ L7 = 15 µH 串联
→ 49.9 Ω 对地终端
```

元件要求：

- 电感在500 kHz时品质因数`Q≥40`；
- 电感自谐振频率不低于10 MHz；
- 电容采用1% C0G/NP0；
- 滤波器输入、走线和输出按50 Ω信号链处理；
- AD9233模块已经是50 Ω输入版本，不得在其SMA端再次并联50 Ω。

设计目标：

| 频率范围 | 目标 |
|---|---|
| 10～500 kHz | 幅度损耗稳定、可校准，无异常陷波 |
| 500 kHz | 固定损耗可重复 |
| 1 MHz | 目标抑制约40 dB |
| 1.1～10 MHz | 无寄生抬升，保持足够抑制 |

### 3.2 放大器

被测信号最小仅50 mVpp，因此在ADC前设置约7.9倍放大。当前使用OPA2810构成
非反相放大器：

```text
滤波器输出
→ 100 nF交流耦合
→ 100 kΩ偏置到2.5 V
→ OPA2810非反相放大
   Rg = 499 Ω
   Rf = 3.83 kΩ
→ 4.7 Ω输出隔离
→ 10 µF交流耦合
→ AD9233 50 Ω输入
```

运算放大器理想闭环增益为：

```text
Av = 1 + Rf/Rg
   = 1 + 3830/499
   ≈ 8.68
```

考虑4.7 Ω隔离电阻和AD9233内部50 Ω负载后，预计输入到ADC的有效电压增益约
为7.9倍：

| 装置BNC输入 | 预计ADC端电压 |
|---:|---:|
| 50 mVpp | 约0.395 Vpp |
| 100 mVpp | 约0.790 Vpp |
| 250 mVpp | 约1.975 Vpp |

上述增益为设计计算值。焊接后必须在50 Ω负载条件下测量10、100、250和
500 kHz的实际增益、相位、噪声、削顶和振荡情况。

## 4. AD9233采集

### 4.1 ADC工作参数

| 参数 | 当前设置 |
|---|---:|
| ADC型号 | 50 Ω输入版AD9233 |
| 分辨率 | 12 bit |
| 原始采样率 | 25.000 MSPS |
| 数据格式 | 二进制补码 |
| 采样边沿 | DCO上升沿 |
| 过量程指示 | OTR |

FPGA提供25 MHz采样时钟，并在上电后通过三线SPI配置AD9233。初始化完成前
保持输出关闭，配置完成后再允许并行数据输出。

### 4.2 AD9233与FPGA连接

| AD9233信号 | FPGA管脚 |
|---|---|
| ADC CLK | T13 |
| DCO | G2 |
| D0～D11 | R10、T11、R9、T10、R8、T9、R7、T8、R6、T7、R5、T6 |
| OTR | R4 |
| OEB | R11 |
| PWDN | R12 |
| CSB | T4 |
| SCLK | R3 |
| SDIO | T5 |

AD9233模块独立接+5 V和GND，并与FPGA、MCU共地。

## 5. FPGA采集与缓存

### 5.1 抽取与缓存参数

| 参数 | 数值 |
|---|---:|
| ADC原始采样率 | 25.000 MSPS |
| 抽取倍数 | 13 |
| 分析采样率 | 1.923076923 MSPS |
| 缓存长度 | 4096点 |
| FFT频格 | 469.501 Hz |
| 一帧原始采集时间 | 2.12992 ms |

13倍抽取表示FPGA每接收13个ADC原始样本，保留其中1个：

```text
fs_analysis = 25 MHz / 13
            = 1.923076923 MHz
```

FFT频格为：

```text
Δf = fs_analysis / 4096
   = 469.501 Hz
```

内部频格小于500 Hz，显示时采用500 Hz网格。最终频率值不是简单取FFT最大
格点，而是经过插值和拟合，因此实测频率误差可以显著小于469.5 Hz。

当前抽取器没有数字FIR滤波，仅执行降采样。因此模拟低通是抗混叠链路的必要
组成部分，而不是可选附件。

### 5.2 缓存工作过程

1. MCU发送`ARM_CAPTURE`；
2. FPGA从下一有效位置开始计数；
3. 每13个原始采样保存1点；
4. 保存4096点后冻结RAM；
5. FPGA置位`frame_ready`并增加`frame_id`；
6. MCU读取整帧并校验CRC；
7. 新的ARM命令到来后才能覆盖缓存。

该方式避免FPGA写RAM与MCU读RAM同时进行，便于保证一帧数据的一致性。

### 5.3 FPGA资源与时序

Quartus II 13.1最终构建记录：

| 项目 | 结果 |
|---|---:|
| 器件 | EP4CE10F17C8 |
| Fitter | Successful |
| 逻辑单元 | 1515 / 10320 |
| RAM | 147456 / 423936 bit |
| PLL | 1 / 2 |
| 最差Setup裕量 | 约+0.902 ns |
| 最差Hold裕量 | 约+0.107 ns |

## 6. FPGA与MCU通信

### 6.1 硬件接口

SPI3采用Mode 2、16位、MSB优先、12 MHz：

| 功能 | STM32H743VI | FPGA | 方向 |
|---|---|---|---|
| SCK | PC10 | E6 | MCU→FPGA |
| MOSI | PB2 | D5 | MCU→FPGA |
| MISO | PC11 | M7 | FPGA→MCU |
| CS_N | PC6 | A8 | MCU→FPGA |
| GND | GND | GND | 共地 |

MISO使用M7的3.3 V I/O Bank。原D6属于2.5 V Bank，不能与同Bank的3.3 V
接口混用。

### 6.2 通信协议

请求帧：

```text
0xA55A + command + argument + CRC16-CCITT
```

命令定义：

| 命令 | 编号 | 功能 |
|---|---:|---|
| ARM_CAPTURE | 0x0001 | 启动一帧采集 |
| GET_STATUS | 0x0002 | 读取状态 |
| READ_BLOCK | 0x0003 | 读取4096点数据 |

数据响应包括：

```text
0x5AA5
协议版本
状态标志
frame_id
采样率
采样点数
OTR计数
4096个16位符号样本
CRC16-CCITT
```

MCU只有在同步字、版本、点数和CRC全部正确时才接收该帧。错误统计包括
`crc`、`protocol`和`dma`。

## 7. MCU分析算法

### 7.1 总体流程

```text
接收4096点
→ 去直流
→ Hann窗
→ 4096点RFFT
→ 搜索10～500 kHz局部峰
→ 频率插值
→ 判断整数谐波关系
→ 优化基频
→ 原始未加窗数据联合最小二乘拟合
→ 幅值、相位、Upp、Urms
→ 波形与频谱显示数据
```

### 7.2 基频和谐波识别

FFT首先提供候选谱峰，再枚举1～50次整数谐波关系。系统选择能使全部有效
谱线最接近整数倍关系的基频`f0`，最多保留3个实际分量。

该方法可以识别：

- 基波 + 二次谐波；
- 基波 + 三次、五次谐波；
- 基波 + 三次、四次谐波；
- 其他1～50次范围内的整数谐波组合。

### 7.3 联合最小二乘拟合

对未加窗的原始数据拟合：

```text
x(t) = DC + Σ[ak·cos(2πnkf0t) + bk·sin(2πnkf0t)]
```

第`k`个分量的峰值为：

```text
Apeak,k = sqrt(ak² + bk²)
```

分量峰峰值为：

```text
Vpp,k = 2·Apeak,k
```

多音周期信号的真有效值为：

```text
Urms = sqrt(Σ Apeak,k² / 2)
```

系统根据拟合得到的幅值和相位，在一个基波周期内重构高密度波形，再计算：

```text
Upp = max(u_rebuild) - min(u_rebuild)
```

不能把各分量峰峰值直接相加作为总Upp，因为总Upp还与各次谐波相位有关。

### 7.4 显示数据

MCU当前支持：

- `WaveRaw`：输出下一帧4096个抽取样本；
- `WaveDisplay`：输出480个拟合重构点；
- 输出Upp、Urms、f0、分量阶次、频率、峰值和相位；
- 输出PLL、ADC配置、OTR、CRC、协议和DMA诊断状态。

USART3使用PD8/PD9，波特率230400、8N1。

## 8. 当前校准方法

### 8.1 临时直连校准

当前固件参数为：

```text
scale = 0.968936
mv_per_code = 0.299439
```

它对应“信号源直接接AD9233，同时用较大ADC端电压模拟未来7.9倍放大器”的
临时台架。该系数只能用于验证ADC、FPGA、SPI和算法，不是整机最终系数。

### 8.2 整机校准方案

接入模拟低通和放大器后，以装置BNC输入端的实际电压为基准，选择：

```text
10 kHz、20 kHz、…、500 kHz
```

共50个校准频点。每个频点记录：

- 装置BNC端实际输入幅值；
- ADC拟合幅值；
- 前端幅度增益；
- 前端相位延迟；
- OTR、噪声和重复性。

生成复频响校准表：

```text
H(f) = gain(f) · exp(j·phase(f))
```

运行时对每个分量的幅值和相位进行插值补偿。校准表应存入MCU Flash，并带
版本号和CRC。

### 8.3 50 Ω测量约定

- 信号源设置`Load=50 Ω`；
- 200 mVpp干扰必须以装置BNC输入端实测值为准；
- 装置BNC正常连接50 Ω前端时，示波器应使用10×高阻探头并接测量点；
- 不应再并联示波器50 Ω输入，否则装置输入负载会变成约25 Ω；
- 不能用两台信号源直接并联产生`ub+uJ`；
- 合成输入应采用单通道ARB、仪器内部谐波功能或50 Ω功率合路器。

## 9. 已完成测试

> 本节是AD9233直连和临时输入折算条件下的实测结果，不含最终模拟前端。

### 9.1 单音

| 频率 | 输入折算目标 | 测量结果 | 结论 |
|---:|---:|---:|---|
| 10 kHz | 约50 mVpp | 约49.6～49.8 mVpp | 通过数字链验证 |
| 150 kHz | 约50 mVpp | 约49.8 mVpp | 通过数字链验证 |
| 500 kHz | 约50 mVpp | 约49.8 mVpp | 通过数字链验证 |
| 10 kHz | 约250 mVpp | 约250.04 mVpp | 通过数字链验证 |
| 150 kHz | 约250 mVpp | 约249.75 mVpp | 通过数字链验证 |
| 500 kHz | 约250 mVpp | 约249.56 mVpp | 通过数字链验证 |

各测试频率误差均远小于1 kHz，`otr=0`。

### 9.2 双音最高频边界

输入目标：

```text
250 kHz基波：100 mVpp
500 kHz二次谐波：50 mVpp
```

实测：

```text
f0 ≈ 249999.7 Hz
n=1 peak ≈ 49.978 mV
n=2 peak ≈ 25.013 mV
Upp ≈ 129.885 mV
Urms ≈ 39.517 mV
processing_us ≈ 74400
otr = 0
```

### 9.3 三音

输入目标：

```text
40 kHz基波：100 mVpp
120 kHz三次谐波：50 mVpp
200 kHz五次谐波：25 mVpp
```

实测：

```text
f0 ≈ 40000.8 Hz
n=1 peak ≈ 49.92 mV
n=3 peak ≈ 24.94 mV
n=5 peak ≈ 12.46 mV
Upp ≈ 112.67 mV
Urms ≈ 40.43 mV
processing_us ≈ 106000
otr = 0
```

### 9.4 通信稳定性

连续运行约1分钟：

```text
pll=1
cfg=1
otr=0
crc=0
protocol=0
dma=0
```

有效帧计数`good`持续增长，未出现CRC、协议或DMA错误。

### 9.5 构建和仿真

| 项目 | 结果 |
|---|---|
| ModelSim SPI协议仿真 | `TB_PASS` |
| Quartus II 13.1 | Fitter Successful，时序裕量为正 |
| Keil MDK | 0 Error、0 Warning |
| FPGA正式文件 | `FPGA_sendwave/prj/DAC904_HighSpeedDAC.sof` |
| MCU正式文件 | `MDK-ARM/Project/Project.hex` |

## 10. 当前完成度

| 子系统 | 状态 | 能否写入报告“实测完成” |
|---|---|---|
| AD9233 25 MSPS采集 | 已上板验证 | 可以 |
| FPGA抽取与4096点缓存 | 已上板验证 | 可以 |
| FPGA→MCU SPI和CRC | 已上板验证 | 可以 |
| FFT、谐波识别和拟合 | 已用单/双/三音验证 | 可以 |
| 频率、Upp、Urms和分量计算 | 已在直连台架验证 | 必须注明直连测试条件 |
| 模拟低通 | 设计完成，实物待测 | 不可以写成已通过 |
| 约7.9倍放大器 | 设计完成，实物待测 | 不可以写成已通过 |
| 整机50点校准 | 待完成 | 不可以 |
| `ub+uJ`抗干扰 | 待完成 | 不可以 |
| 1/3周期HMI显示 | MCU数据已有，HMI待集成 | 不可以写成整机完成 |

## 11. 下一步测试方案

### 11.1 低通滤波器单独测试

使用50 Ω源和50 Ω负载，测量：

```text
通带：10、100、250、500 kHz
阻带：1、1.1、1.5、2、5、10 MHz
```

记录每点：

- 输入端实测Vpp；
- 输出端实测Vpp；
- 增益`20log10(Vout/Vin)`；
- 相位差；
- 波形失真；
- 仪器设置和照片编号。

重点检查500 kHz损耗的重复性、1 MHz抑制是否接近40 dB，以及5～10 MHz有无
寄生抬升。

### 11.2 放大器单独测试

在50 Ω负载下依次输入50、100和250 mVpp，测试10、100、250和500 kHz：

- 实际增益；
- 输出偏置；
- 噪声；
- 削顶；
- 高频振荡；
- 频率和幅值重复性。

### 11.3 整机校准

模拟前端接入后完成10～500 kHz共50点幅频和相频校准，再重新测试：

- 50 mVpp最小幅度；
- 250 mVpp最大幅度；
- 10 kHz最低频率；
- 500 kHz最高频率；
- 单音、双音和三音。

### 11.4 抗干扰测试

先仅输入`ub`记录基准，再叠加200 mVpp干扰：

```text
fJ = 1、1.1、1.5、2、5、10 MHz
```

第一组建议：

```text
ub = 100 kHz、100 mVpp
uJ = 1 MHz、200 mVpp
```

该组合可直接使用SDG1062X的谐波功能，在CH1设置100 kHz基波和第10次谐波，
不需要把两个信号源并联。非整数倍或高于第16次谐波的组合使用EasyWave
单通道ARB。

加干扰前后比较：

| 项目 | 通过条件 |
|---|---:|
| `ΔUpp` | 绝对值不超过5 mV |
| `ΔUrms` | 绝对值不超过5 mV |
| 每个`Δpeak_mv` | 绝对值不超过5 mV |
| `Δf0` | 绝对值不超过1 kHz |
| 分量数量和阶数 | 不变 |
| 错误识别 | 不得把混叠干扰识别成有效谐波 |
| 链路状态 | `otr/crc/protocol/dma=0` |
| 总时间 | 小于2 s |

### 11.5 HMI集成

最终显示页面包括：

1. 波形页：1个或3个完整周期、Upp、Urms、f0；
2. 频谱页：0～500 kHz正频率谱和最多3个分量；
3. 诊断页：PLL、ADC配置、OTR、CRC、帧号和处理时间。

高频波形应使用拟合参数重构的480点数据，不直接将1.923 MSPS抽取样本连线，
以保证500 kHz波形显示平滑。

## 12. 误差预算

| 误差来源 | 当前控制方法 | 状态 |
|---|---|---|
| 信号源幅值和50 Ω负载误差 | 以装置BNC实测值为准 | 待整机执行 |
| 低通通带损耗 | 50点复频响校准 | 待完成 |
| 放大器增益和平坦度 | 扫频和复频响校准 | 待完成 |
| ADC量化和噪声 | 放大约7.9倍，提高小信号码数 | 待整机复测 |
| 采样时钟误差 | FPGA PLL和拟合频率估计 | 数字链已验证 |
| FFT泄漏 | Hann窗和峰值插值 | 已实现 |
| FFT格点误差 | 联合最小二乘优化频率 | 已实现 |
| 多音Upp相位影响 | 按拟合幅相重构后求max-min | 已实现 |
| 强干扰混叠 | 抽取前模拟低通 | 待实测 |
| 温漂和重复性 | 多次采集、校准版本和CRC | 待整机验证 |

整机内部误差目标建议控制在2.5 mV以内，为题目5 mV绝对误差指标保留约2倍
余量。

## 13. 主要风险与回退

| 风险 | 触发证据 | 处理或回退 |
|---|---|---|
| 1 MHz抑制不足 | 加干扰后幅值变化超过5 mV | 调整低通元件或增加滤波阶数 |
| 500 kHz通带损耗过大 | 校准后噪声明显放大 | 优化电感Q值和截止频率 |
| 放大器振荡 | 无输入仍有高频输出或OTR | 改善去耦、布局和输出隔离 |
| ADC削顶 | `otr>0`或码值顶格 | 降低增益或增加输入衰减 |
| SPI错误 | `crc/protocol/dma`增加 | 降速、检查共地和连线 |
| HMI开发延误 | 算法已完成但页面未完成 | 保留串口参数显示作为调试回退 |
| 整机校准失效 | 换硬件或温漂后误差增大 | 校准表加版本/CRC并保留重新校准入口 |

## 14. 报告撰写注意事项

报告中可以写成“已完成并实测”的内容：

- AD9233在25 MSPS下稳定采集；
- FPGA 13倍抽取和4096点冻结缓存；
- FPGA与H743的SPI DMA及CRC通信；
- 10～500 kHz单音识别；
- 双音和三音整数谐波识别；
- Upp、真Urms、基频和分量幅值算法；
- 当前数字链路的处理时间与稳定性。

报告中必须写成“设计方案”或“待整机验证”的内容：

- 7阶模拟低通的最终通带和阻带实测值；
- OPA2810放大器的最终实际增益；
- 接入模拟前端后的5 mV整机精度；
- 200 mVpp、1～10 MHz干扰抑制；
- 最终HMI显示效果。

不得把ADC直连时的`0.968936`校准系数作为最终整机校准结果，也不得在模拟
低通没有完成前声称装置已经满足强干扰抑制要求。

## 15. 工程文件

- GitHub仓库：`https://github.com/Ron-Loski/26Diansai_G.git`
- 工作分支：`codex/ad9233-analyzer-verified-stage1`
- MCU工程：`MDK-ARM/Project.uvprojx`
- FPGA工程：`FPGA_sendwave/prj/DAC904_HighSpeedDAC.qpf`
- MCU程序：`MDK-ARM/Project/Project.hex`
- FPGA程序：`FPGA_sendwave/prj/DAC904_HighSpeedDAC.sof`
- AI工程交接：`AI_HANDOFF.md`
- 本方案文档：`CURRENT_SOLUTION_REPORT.md`

## 16. 下一项最有价值的物理实验

立即单独测试7阶模拟低通滤波器：

1. 信号源和负载均按50 Ω设置；
2. 先测10、100、250、500 kHz通带；
3. 再测1、1.1、1.5、2、5、10 MHz阻带；
4. 保存装置输入和滤波输出的示波器截图；
5. 确认500 kHz损耗稳定且1 MHz抑制接近40 dB；
6. 低通通过后再接放大器和ADC。

该实验直接决定13倍抽取是否会受到强干扰混叠影响，是当前最需要优先消除的
整机不确定性。
